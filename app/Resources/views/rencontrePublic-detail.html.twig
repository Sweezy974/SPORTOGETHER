{% extends "FOSUserBundle::layout.html.twig" %}

{% trans_default_domain 'FOSUserBundle' %}

{% block fos_user_content %}
<form class="col-md-offset-3 col-md-6 text-center">
  <h3 style="text-transform:uppercase;">nom groupe</h3>
  <h4>{{ rencontrePublic.nomGroupe }}</h4>
  <h3 style="text-transform:uppercase;">sport</h3>
  <h4>{{ rencontrePublic.sport }}</h4>
  <h3 style="text-transform:uppercase;">description</h3>
  <h4>{{ rencontrePublic.description }}</h4>
  <h3 style="text-transform:uppercase;">lieu</h4>
  <h4>{{ rencontrePublic.lieu }}</h4>
  <h3 style="text-transform:uppercase;">date</h3>
  <h4>{{ rencontrePublic.date|date("d/m/Y")}}</h4>
  <div class="col-md-12 text-center">
    <a href= "{{ path ('sportogether_join-rencontre' ,{'id': rencontrePublic.id}) }}"><button class="btn-primary" type="button" name="button">Participer à la rencontre</button></a>
  </div>

</form>


<div class="col-md-12 text-center">
  <a href="{{ path('RencontrePublic_show') }}"><button class="btn-primary" type="button" name="button">Retour Rencontre Public</button></a>
</div>

{% endblock fos_user_content %}
